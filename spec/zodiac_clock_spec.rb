require "spec_helper"

RSpec.describe ZodiacClock do
  it "has a version number" do
    expect(ZodiacClock::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
